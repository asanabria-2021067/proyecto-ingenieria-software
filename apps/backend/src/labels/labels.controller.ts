import { Controller } from '@nestjs/common';

/**
 * Tarea 30: estructura y prefijo contextualizado por proyecto únicamente.
 * Sin handlers todavía — el CRUD (listar/crear/editar/eliminar etiquetas)
 * es alcance de una tarea posterior.
 */
@Controller('proyectos/:projectId/etiquetas')
export class LabelsController {}
