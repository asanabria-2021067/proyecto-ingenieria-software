name: CI

# Se ejecuta en tres escenarios:
#   1. pull_request  -> valida cada PR hacia main o develop
#   2. push          -> valida cada push directo a develop
#   3. workflow_call -> lo invoca deploy.yml antes de desplegar a produccion
on:
  pull_request:
    branches: [main, develop]
  push:
    branches: [develop]
  workflow_call:
  workflow_dispatch:

# Si se empujan varios commits seguidos al mismo PR, cancela las corridas
# anteriores y deja solo la ultima.
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

env:
  # Apunta al servicio postgres del job backend (ver services: mas abajo).
  # Mismas credenciales ci/ci/ci en ambos lados para no duplicar valores.
  DATABASE_URL: postgresql://ci:ci@localhost:5432/ci?schema=public
  DIRECT_URL: postgresql://ci:ci@localhost:5432/ci?schema=public

jobs:
  backend:
    name: Backend - lint y pruebas
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/backend
    services:
      postgres:
        image: postgres:17-alpine
        env:
          POSTGRES_USER: ci
          POSTGRES_PASSWORD: ci
          POSTGRES_DB: ci
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U ci -d ci"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
    steps:
      - name: Checkout del repositorio
        uses: actions/checkout@v4

      - name: Configurar Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: apps/backend/package-lock.json

      - name: Instalar dependencias
        run: npm ci

      - name: Generar cliente Prisma
        run: npx prisma generate

      - name: Aplicar migraciones a la base de CI
        run: npx prisma migrate deploy

      - name: Lint
        run: npm run lint
        continue-on-error: true

      - name: Pruebas unitarias (Vitest)
        run: npm run test

      - name: Pruebas de integracion con PostgreSQL real (Vitest, en serie)
        run: npm run test:integration
        env:
          # Corren en serie (vitest.integration.config.ts) para no pisarse
          # entre si contra la misma base: varias hacen TRUNCATE/limpieza.
          ROLES_IT_DATABASE_URL: postgresql://ci:ci@localhost:5432/ci?schema=public
          INTEGRATION_DATABASE_URL: postgresql://ci:ci@localhost:5432/ci?schema=public
          RUN_REAL_DB_TESTS: '1'

  frontend:
    name: Frontend - lint y pruebas
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/frontend
    steps:
      - name: Checkout del repositorio
        uses: actions/checkout@v4

      - name: Configurar Node.js 22
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: apps/frontend/package-lock.json

      - name: Instalar dependencias
        run: npm ci

      - name: Lint
        run: npm run lint
        continue-on-error: true

      - name: Pruebas (Vitest)
        run: npm run test
        